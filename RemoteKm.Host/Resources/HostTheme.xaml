<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Palette (matches the client) -->
    <Color x:Key="RkBg">#0F1020</Color>
    <Color x:Key="RkSurface">#16172B</Color>
    <Color x:Key="RkSurfaceAlt">#232544</Color>
    <Color x:Key="RkStroke">#2C2F52</Color>
    <Color x:Key="RkAccent">#6C5CE7</Color>
    <Color x:Key="RkAccent2">#00D2A8</Color>
    <Color x:Key="RkSuccess">#2EC27E</Color>
    <Color x:Key="RkDanger">#FF5C72</Color>
    <Color x:Key="RkText">#F2F3FB</Color>
    <Color x:Key="RkMuted">#9AA0C0</Color>

    <SolidColorBrush x:Key="RkBgBrush" Color="{StaticResource RkBg}" />
    <SolidColorBrush x:Key="RkSurfaceBrush" Color="{StaticResource RkSurface}" />
    <SolidColorBrush x:Key="RkSurfaceAltBrush" Color="{StaticResource RkSurfaceAlt}" />
    <SolidColorBrush x:Key="RkStrokeBrush" Color="{StaticResource RkStroke}" />
    <SolidColorBrush x:Key="RkAccentBrush" Color="{StaticResource RkAccent}" />
    <SolidColorBrush x:Key="RkSuccessBrush" Color="{StaticResource RkSuccess}" />
    <SolidColorBrush x:Key="RkDangerBrush" Color="{StaticResource RkDanger}" />
    <SolidColorBrush x:Key="RkTextBrush" Color="{StaticResource RkText}" />
    <SolidColorBrush x:Key="RkMutedBrush" Color="{StaticResource RkMuted}" />

    <!-- Rounded, hover-aware filled button -->
    <Style x:Key="RkButton" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource RkTextBrush}" />
        <Setter Property="Background" Value="{StaticResource RkSurfaceAltBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Height" Value="38" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" CornerRadius="10" Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.88" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.72" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="RkAccentButton" TargetType="Button" BasedOn="{StaticResource RkButton}">
        <Setter Property="Background" Value="{StaticResource RkAccentBrush}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="RkSuccessButton" TargetType="Button" BasedOn="{StaticResource RkButton}">
        <Setter Property="Background" Value="{StaticResource RkSuccessBrush}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="RkDangerButton" TargetType="Button" BasedOn="{StaticResource RkButton}">
        <Setter Property="Background" Value="{StaticResource RkDangerBrush}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <!-- Caption close button -->
    <Style x:Key="RkCloseButton" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource RkMutedBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="Width" Value="34" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" CornerRadius="8" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource RkDangerBrush}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Rounded text box -->
    <Style x:Key="RkTextBox" TargetType="TextBox">
        <Setter Property="Foreground" Value="{StaticResource RkTextBrush}" />
        <Setter Property="CaretBrush" Value="{StaticResource RkTextBrush}" />
        <Setter Property="Background" Value="{StaticResource RkBgBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource RkStrokeBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Padding" Value="10,7" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border CornerRadius="8" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Modern check box -->
    <Style x:Key="RkCheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource RkTextBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="Box" Width="22" Height="22" CornerRadius="6"
                                Background="{StaticResource RkBgBrush}"
                                BorderBrush="{StaticResource RkStrokeBrush}" BorderThickness="1"
                                VerticalAlignment="Center">
                            <Path x:Name="Check" Width="11" Height="11" Stretch="Uniform"
                              Data="M 0,5 L 4,9 L 11,1" Stroke="White" StrokeThickness="2"
                              StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                              StrokeLineJoin="Round" Visibility="Collapsed"
                              HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="{StaticResource RkAccentBrush}" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource RkAccentBrush}" />
                            <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Title / heading text -->
    <Style x:Key="RkTitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource RkTextBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI Semibold" />
        <Setter Property="FontSize" Value="18" />
    </Style>

    <Style x:Key="RkCaption" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource RkMutedBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <!-- ===== Modern scrollbar (applies app-wide) ===== -->

    <Style x:Key="RkScrollThumb" TargetType="{x:Type Thumb}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border x:Name="Bd" CornerRadius="4" Margin="2"
                            Background="{StaticResource RkStrokeBrush}" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource RkMutedBrush}" />
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource RkAccentBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Width" Value="12" />
        <Setter Property="MinWidth" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid x:Name="GridRoot" Background="{TemplateBinding Background}">
                        <Track x:Name="PART_Track" IsDirectionReversed="True" Focusable="False">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False" />
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource RkScrollThumb}" />
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False" />
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter TargetName="GridRoot" Property="LayoutTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="-90" />
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
