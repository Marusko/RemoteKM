<Window x:Class="RemoteKm.Host.Windows.MessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RemoteKM"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        FontFamily="Segoe UI"
        Background="{StaticResource RkBgBrush}"
        Icon="/icon.ico">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Ellipse x:Name="SeverityDot" Width="12" Height="12"
                     Fill="{StaticResource RkAccentBrush}" VerticalAlignment="Center" />
            <TextBlock x:Name="TitleText" Style="{StaticResource RkTitle}" Margin="10,0,0,0" />
        </StackPanel>

        <TextBlock Grid.Row="1" x:Name="MessageText" Foreground="{StaticResource RkTextBrush}"
                   FontSize="14" TextWrapping="Wrap" Margin="0,0,0,18" />

        <Button Grid.Row="2" Content="OK" Width="110" Height="42" HorizontalAlignment="Right"
                Style="{StaticResource RkAccentButton}" Click="OnOk" IsDefault="True" />
    </Grid>
</Window>
