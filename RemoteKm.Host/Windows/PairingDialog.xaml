<Window x:Class="RemoteKm.Host.Windows.PairingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RemoteKM — Pairing request"
        Width="460" Height="270"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="True"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        FontFamily="Segoe UI"
        Background="{StaticResource RkBgBrush}"
        Icon="/icon.ico">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Ellipse Width="12" Height="12" Fill="{StaticResource RkAccentBrush}" VerticalAlignment="Center" />
            <TextBlock Text="Pairing request" Style="{StaticResource RkTitle}" Margin="10,0,0,0" />
        </StackPanel>

        <TextBlock Grid.Row="1" x:Name="MessageText" Margin="0,16,0,0"
                   Foreground="{StaticResource RkTextBrush}" FontSize="15"
                   TextWrapping="Wrap" VerticalAlignment="Center" />

        <TextBlock Grid.Row="2" x:Name="CountdownText" Margin="0,0,0,16" Style="{StaticResource RkCaption}" />

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Name="AcceptButton" Content="Accept" Height="46"
                    FontSize="15" Style="{StaticResource RkSuccessButton}" Click="OnAccept" />
            <Button Grid.Column="2" x:Name="RejectButton" Content="Reject" Height="46"
                    FontSize="15" Style="{StaticResource RkDangerButton}" Click="OnReject" />
        </Grid>
    </Grid>
</Window>
